# Frontend env vars consumed by Vite at build time. Vercel reads this
# file during `npm run build` if you don't override the values in the
# Vercel project dashboard.
#
# Production deploys serve the SPA and API from the same Vercel
# domain, so the API base is a relative path.

VITE_API_BASE_URL=/api/v1
