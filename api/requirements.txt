# Python dependencies bundled with the Vercel serverless function.
# Mirrors backend/requirements.txt but trims dev/test packages to stay
# under the 50 MB unzipped function size limit on the Vercel Hobby plan.

fastapi>=0.111,<0.116
uvicorn[standard]>=0.30,<0.32
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
sqlalchemy>=2.0.30,<2.1
psycopg2-binary>=2.9.9
httpx>=0.27,<0.28
python-jose[cryptography]>=3.3,<4
passlib[bcrypt]>=1.7.4,<2
python-multipart>=0.0.9
email-validator>=2.2,<3

# Export engine (DOCX / XLSX / PPTX)
python-docx>=1.1,<2
openpyxl>=3.1,<4
python-pptx>=1.0,<2

# Literature uploads (PDF parsing)
pypdf>=4,<7
