[pytest]
pythonpath = .
testpaths = tests
addopts = -q
# Project tests start with `test_`; the prefix `Test` collides with Pydantic
# schemas like `TestRecommendationRequest`, so restrict class collection.
python_classes = Test*Case
